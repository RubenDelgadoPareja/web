## 1. Baseline MCP Configuration

- [ ] 1.1 Define and create a baseline MCP configuration file for Copilot CLI.
- [ ] 1.2 Document how to load project-specific MCP config using `--additional-mcp-config`.

## 2. Security and Permissions

- [ ] 2.1 Define explicit allow/deny tool and URL defaults for MCP-enabled sessions.
- [ ] 2.2 Document when unrestricted modes (`--allow-all`, `--yolo`) are acceptable and when they are not.

## 3. Validation Workflow

- [ ] 3.1 Add a startup validation checklist to confirm MCP servers and tools are available.
- [ ] 3.2 Validate configuration with a real Copilot CLI session and capture the expected verification output.
